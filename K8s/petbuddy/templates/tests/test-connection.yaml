apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "petbuddy.fullname" . }}-test-connection"
  labels:
    {{- include "petbuddy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "petbuddy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
